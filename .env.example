# Copy this file to .env and adjust as needed.
# .env is gitignored and never committed.

# Set to true to skip authentication when running on localhost.
# Remove or set to false to test the full auth flow locally.
DEV_BYPASS=true

# Only used on Deno Deploy (DENO_DEPLOYMENT_ID must be set).
# Configure these in Deno Deploy project settings, not here.
# ADMIN_USERNAME=
# ADMIN_PASSWORD=
